class StorageObjectModel {
	constructor(cityName, coord, currentWeather, currentTemps) {
		this.cityName = cityName;
		this.coord = coord;
		this.currentWeather = currentWeather;
		this.currentTemps = currentTemps;
	}
}

export default StorageObjectModel;
